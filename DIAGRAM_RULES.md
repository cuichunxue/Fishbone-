# 特性要因図（石川ダイアグラム）自動生成 ルール・パラメータ仕様書

本書は `ishikawa-diagram.js` が特性要因図を自動生成する際の**描画ルール**と**レイアウトアルゴリズム**、および**全パラメータ**を明文化したものです。コードの実装と対応しており、レイアウト調整時のリファレンスとして使用します。

---

## 1. 用語・階層構造

特性要因図は以下の 5 階層で構成されます。

| 階層 | 呼称 | 役割 | データ上のキー |
|---|---|---|---|
| 0 | 特性 (effect) | 分析対象の問題・結果。図の右端の赤い箱 | `effect` |
| 1 | 背骨 (spine) | 中心の太い水平矢印。特性ボックスへ向かう | - |
| 2 | 大骨 (major bone) | カテゴリ（4M: 機械・人・材料・方法 等）を表す斜めの骨 | `category` |
| 3 | 中骨 (cause) | 大骨に対する原因 | `cause` |
| 4 | 小骨 (subcause) | 中骨に対する副原因 | `subcause` |
| 5 | 孫骨 (detail) | 小骨に対する詳細要因 | `detail` |

各階層は「親骨」（1 つ上の階層の骨）に対して枝分かれする「子骨」であり、レイアウトルールは**同じ再帰パターン**を各階層に繰り返し適用する形で設計されています。

---

## 2. 描画の基本ルール（教科書ルール）

1. **背骨**: 太い水平の矢印。矢先は特性ボックスの左辺に接する。
2. **大骨**: 背骨に対して 60°（`majorAngleDeg`）で、カテゴリの並び順に上下交互に配置。矢先は背骨に突き刺さる。先端にカテゴリボックスを置く。
3. **中骨**: 背骨と平行（水平）。大骨の両側にペアで交互配置し、矢先は大骨へ向かう。ペアの左右は大骨上でわずかに段違い（スタガー）にし、矢印同士の衝突を防ぐ。
4. **小骨**: 大骨と平行（大骨と同じ 60°）。中骨の上下に交互配置し、矢先は中骨へ向かう。
5. **孫骨**: 中骨と平行（水平）。小骨のライン上に分岐し、矢先は小骨へ向かう。
6. **線の太さ**: 階層が下がるごとに単調減少する。
   - 背骨 6.0 → 大骨 4.2 → 中骨 2.6 → 小骨 1.8 → 孫骨 1.2
7. **矢印サイズ**: 背骨 20 → 大骨 12 → 中骨 10 → 小骨 8 → 孫骨 6

### 2.1 再帰的な交互展開ルール

> **「親骨が斜めなら子骨は左右交互、親骨が水平なら子骨は上下交互」**

この 1 つのルールを階層に関わらず繰り返し適用している。

| 親骨 | 親骨の向き | 子骨の交互方向 |
|---|---|---|
| 背骨 | 水平 | 大骨は上下交互（カテゴリの出現順） |
| 大骨 | 斜め (60°) | 中骨は左右交互 |
| 中骨 | 水平 | 小骨は上下交互 |
| 小骨 | 斜め (60°) | 孫骨は左右交互（※安全な場合のみ。2.2 参照） |

### 2.2 孫骨の左右交互展開の安全制限

孫骨は本来「小骨が斜め骨なので左右交互」となるはずだが、内側配置の小骨で行うと大骨ラインに干渉するケースがあるため、以下の条件を**すべて**満たす場合のみ交互展開を許可する。

- 中骨が **外側 (left)** に配置されている
- 小骨が **外側 (isOuterSub)** に配置されている
- 反転した孫骨が大骨ラインに `innerSafeMargin` 未満まで接近しない（`segmentTooCloseToLine` による幾何判定）
- 反転した孫骨が同じ側の隣接小骨の領域を侵さない（`maxSafeFlipReach` の 46% 以内）

条件を満たさない場合は、孫骨は親小骨と同じ方向（安全側）に統一する。

### 2.3 中骨の左右アサイン（内容量ベース）

中骨は大骨の両側に「ペア」単位で配置され、ペア内の片方が外側 (left)、もう片方が内側 (right / 背骨側) になる。**どの中骨をどちらの側にするか**は、以下のルールで内容量に応じて決定する（詳細は 3.5 節）。

- 内側は背骨に近いためクリアランスが厳しい。**必要スペースが小さい中骨を内側、大きい中骨を外側**に割り当てる。
- L, R, L, R... という**位置の交互サイクル**は保つが、その位置をどの中骨（データ上のどの原因）が占めるかは内容量順に決める。

---

## 3. 自動レイアウトアルゴリズム

完全データ駆動で、描画前に全座標を計算する。どんなデータでも重ならないキャンバスサイズと配置を自動決定する（テキスト幅も推定して反映）。

### 3.1 全体のフェーズ構成 (`computeLayout`)

1. **Phase 1（`categoryBasics`）**: カテゴリ・原因ごとに「必要な長さ」の材料（`causeMetrics`）を計算。ラベル幅、小骨/孫骨の本数などを集計するのみで、まだ長さは確定しない。
2. **Phase 2（グローバル長さの統一）**: 中骨長・小骨長を、それぞれの階層で図全体の最大値に統一する。
3. **Phase 3（`categoryInfos`）**: 統一された長さを使い、各カテゴリの縦横の張り出し量、中骨ペアの左右割当て、大骨長・ペア位置の元になる必要量を算出する。
4. **図全体で統一するペア位置の決定**: 全カテゴリの必要量を集約し、大骨長 L とペアの t 位置（`globalPairTs`）を 1 組だけ計算し、全カテゴリで共有する。
5. **キャンバスサイズ確定**: カテゴリの縦横の張り出しから svg の高さ・幅を決定し、最低アスペクト比を保証する。
6. **座標確定（`computeCategoryGeometry` 等）**: シフト後の座標を使い、各カテゴリ・中骨・小骨・孫骨の実座標を計算する。

### 3.2 「同じ層の親骨は図全体で統一」の原則

これは本アルゴリズム全体を貫く中心的な設計原則である。

> 中骨・小骨・大骨は、それぞれの階層内で「最も子骨数が多い（＝最も長さを必要とする）もの」を基準に、**同じ階層のすべての骨に同じ長さ**を適用する。

これにより、階層ごとに骨の長さが揃い、子が少ない骨だけが不釣り合いに間延びする「スカスカ」を防ぎつつ、図全体で一貫したバランスの取れた見た目になる。

- **大骨長** (`majorBoneLength`): 全カテゴリで統一 → 大骨先端の Y 座標が揃う。
- **中骨長** (`globalCauseLength` = `causeLength`): 図全体の全中骨で統一。
- **小骨長** (`globalSubLen` = `subLenLong`): 図全体の全小骨で統一。
- **中骨ペアの t 位置** (`globalPairTs`): 図全体の全カテゴリで統一（詳細は 3.6 節）。

> **重要な注意**: 統一の基準は常に「**子骨の本数**」であり、「親骨自身のラベル文字数」ではない。ラベル幅を長さの決定に混ぜると、1 つだけ極端に長いラベルの要因が図全体の骨を間延びさせてしまう（子骨が少ない他の要因まで無駄に長い骨になる、別種のスカスカ）。ラベル幅は骨がラベルより短い場合に単に先端からはみ出して描画されるだけなので、**キャンバスサイズ計算にのみ使用**し、骨の長さ自体の決定には使わない。

### 3.3 骨の長さの決定式

子骨のペア数 (`ceil(N/2)`) に応じて段階的に伸縮する。

```
中骨長 = max(causeBaseLength, ...全中骨の max(subSpread, maxSubLabel + 40))
  ただし各中骨の subSpread = max(
    ラベル安全間隔から算出した必要長,
    causeBaseLength + max(0, ceil(小骨数/2) - 1) * causeLengthPerPair
  )

小骨長 = max(subcauseLength, ...全小骨の subLen)
  ただし各小骨の subLen = subcauseLength + max(0, ceil(孫骨数/2) - 1) * subcauseLengthPerPair
```

### 3.4 「親骨の起点側から詰めて展開する」配置モデル

これも全階層で共通する設計原則である。

> 子骨は「親の親（1 つ上の階層）に近い側」を起点 (anchor) とし、そこから**実際に必要な物理間隔の分だけ**順に詰めて配置する。子が少なければ起点付近にまとまり、子が多いほど先端側へ伸びる。

これは「均等配置（`computeEvenT`：固定範囲に常に均等分散）」の対極にある考え方で、旧方式では原因数が奇数・少数のカテゴリで中間に大きな空白ができる「スカスカ」の原因になっていたため、この方式に置き換えられた。

各階層での「起点」と「展開方向」は以下の通り。

| 子骨 | 親骨 | 起点 (anchor) | 展開方向 |
|---|---|---|---|
| 中骨 | 大骨 | `pairTAnchor`（背骨側） | 先端（カテゴリボックス）側へ増加 |
| 小骨 | 中骨 | `subcauseTMax`（大骨側 = 中骨の付け根） | 中骨の自由端側へ減少 |
| 孫骨 | 小骨 | `detailTMin`（中骨側 = 小骨の付け根） | 小骨の自由端側へ増加 |

汎用実装は `computeAnchoredPackedT(n, anchorT, stepPx, parentLenPx, dirSign, tBound)`。

- 自然な（詰めた）配置が範囲 `tBound` を超える場合は、**全体を比例縮小**して最後の点がちょうど `tBound` に収まるようにする。単純な per-step クランプだと複数点が同じ境界値に重なりラベルが完全一致・重複してしまうため、必ず不等間隔を保つ設計になっている。
- 十分な余地がない極端なケースでは、結果的に旧来の均等配置と同じ結果に収束する（安全側にフォールバックする）。

中骨（ペア配置）専用の実装は `computePairPackedT(numPairs, L, verticalNeed, innerNeedByPairIndex, sinA, cosA)` で、ペアごとに「内側配置時の必要クリアランス」を考慮しながら同様のロジックで詰める。

### 3.5 中骨のペア組み・左右割当て（内容量ソート）

1. 各中骨について「内側（背骨側）に配置したときの必要長」`innerNeedOf(m)` を計算する。
   ```
   ownLen = max(70, 小骨本数 > 0 ? 小骨レイアウト長 * 0.92 : 0)
   tipExtrasOf(m) = max(
     (小骨本数 > 0 ? 小骨長 * cos(60°) : 0) + (孫骨あり ? detailLength + 最大孫骨ラベル幅 + 16 : 0),
     中骨ラベル幅 + 12
   )
   innerNeedOf(m) = ownLen + tipExtrasOf(m) + innerSafeMargin
   ```
   （`ownLen` の係数 0.92 は、実行時の安全キャップ（3.7 節）が別途働くことを前提に、大骨長の過大な事前確保を避けるための緩和係数。）
2. 全中骨を `innerNeedOf` の**昇順**にソートする。
3. 軽い（必要長が小さい）中骨同士を**背骨に一番近いペア**に、重い中骨同士を**先端側の遠いペア**（元々余地が大きい）にまとめる。
4. 各ペア内では、必要長が小さい方を **内側 (right)**、大きい方を **外側 (left)** にする。
5. 相方のいない単独中骨（原因数が奇数の余り）は常に外側 (left)。

L, R, L, R... という**位置の交互サイクル**は大骨上の位置の順序であり、その位置をどの中骨（原因）が占めるかとは独立なので、この並び替えを行っても交互サイクルは崩れない。

### 3.6 大骨長・ペア位置の図全体統一

1. 全カテゴリ（ペア配置のもの）の `innerNeedByPairIndex`（ペア番号 k ごとの必要クリアランス）と `verticalNeedBetweenCauses`（中骨間の縦方向必要間隔）を集約し、**ペア番号 k ごとの最大値**を取る（`globalInnerNeedByPairIndex`, `globalVerticalNeed`）。
2. 以下の 2 条件をともに満たす最小の大骨長 L を二分探索する（条件は L が大きいほど満たしやすいため単調）。
   - 最後のペアの t が `pairTMax` を超えない
   - カテゴリボックスまでの絶対距離が `pairBoxClearance` (px) 以上ある
3. 決定した L と集約済みの必要量を使って、`globalPairTs`（各ペアの t 位置）を **1 組だけ**計算する。
4. 全カテゴリがこの同じ `globalPairTs` を使う。ペア数が少ないカテゴリは、共有 `globalPairTs` の先頭（背骨側）から必要な分だけ使う。

これにより、カテゴリごとに内容（ラベル長・小骨/孫骨の量）が多少違っても、**中骨の配置バランスが全カテゴリで完全に揃う**（あるカテゴリだけ背骨付近に詰まり、別のカテゴリは間延びする、という不揃いを防ぐ）。

### 3.7 内側（背骨側）中骨・小骨の実行時安全キャップ

内側へ伸びる中骨・小骨は、背骨に届かない長さに実行時に自動キャップされる（3.5 節の事前予約とは別の安全弁として機能）。

```
中骨（内側）:
  ownTipExtras = max(小骨長*cos(60°) + 孫骨チェーンの張り出し, 中骨ラベル幅+12) + innerSafeMargin
  distToSpine  = 背骨X - attachX
  maxInner     = distToSpine - ownTipExtras
  causeLen     = max(80, min(統一中骨長, maxInner))

小骨（内側）:
  innerSubMaxLen = max(40, (中骨Yと背骨Yの距離 - innerSafeMargin) / sin(60°))
  cap  = min(通常小骨長, innerSubMaxLen)
  長さ = 孫骨あり ? cap : cap * subcauseStaggerRatio
```

### 3.8 カテゴリの配置（列・上下対称）

- カテゴリは入力順で上下交互（`idx % 2` で isTop 判定）に振り分けられる（4M の慣習「機械・人・材料・方法」なら機械/材料が上段、人/方法が下段）。
- 連続する 2 件（`idx=2k` を上、`idx=2k+1` を下）を「同じ列」として扱い、同じ `spineX`（背骨上のX座標）を共有させる。これにより上下対称・左右整列の「教科書通りの 4M レイアウト」を実現する。
- 背骨は常にキャンバスの上下中央に配置し、左右対称な見た目を維持する。
- 最低アスペクト比 1.15 を保証する（`minAspect`）。縦長になりすぎる場合は水平方向にパディングを追加してバランスを取る。

### 3.9 ラベルの折り返しルール

`labelWrapWidth`（4 節参照）を超えるラベルは、文字幅推定に基づいて**最大 2 行**にバランスよく分割する（`wrapLabel`）。カテゴリ名も同様に折り返し対象で、長い場合はカテゴリボックス自体を拡幅・拡高する（固定幅のままだと長い名前が箱からはみ出しキャンバス外で切れてしまうため）。

文字幅推定 (`estimateTextWidth`) は日本語・英数字混在に対応し、半角 ASCII は約 0.55em、全角文字は約 1.0em として計算する。

### 3.10 重要要因マーカー（赤丸）

`cause` / `subcause` / `detail` のラベル末尾に `!`（または全角 `！`）を付けると、その要因が**赤い楕円**で囲まれる（QC の慣習「重要と思われる要因を丸で囲む」を表現）。パース処理は `mermaid-parser.js` の `parseImportance()` が担当する。

例: `cause "技能不足!"` → 表示名は「技能不足」、赤丸付きで描画。

---

## 4. パラメータ一覧 (`this.params`)

### 4.1 角度

| パラメータ | 値 | 説明 |
|---|---|---|
| `majorAngleDeg` | 60 | 大骨の背骨に対する傾き（教科書標準） |
| `subcauseAngleDeg` | 60 | 小骨の傾き（大骨と平行、教科書ルール） |

### 4.2 中骨の t 範囲（大骨上の位置パラメータ、両側ペア配置）

| パラメータ | 値 | 説明 |
|---|---|---|
| `pairTAnchor` | 0.16 | 最初のペアが取り得る最小 t（背骨に最も近い位置）。ここから必要な間隔だけ先端側へ詰めて展開する |
| `pairTMax` | 0.86 | ペアの t が超えてはいけない上限（この先はカテゴリボックスの手前の余白） |
| `pairBoxClearance` | 150 | 大骨長 L が小さい図で、ペアが `pairTMax` まで詰まっても確保すべきカテゴリボックスまでの最低絶対距離 (px)。小骨・重要マーク楕円がボックスに接触しないための安全弁 |
| `pairStaggerT` | 0.08 | ペアの左右段差（t 単位）。右（内側）を先端寄りにずらし、矢印同士の衝突を防ぐ |

### 4.3 中骨の t 範囲（片側配置フォールバック、原因数 1 本のカテゴリのみ）

| パラメータ | 値 | 説明 |
|---|---|---|
| `singleTMin` | 0.18 | 片側配置の t 最小値 |
| `singleTMax` | 0.84 | 片側配置の t 最大値 |

### 4.4 小骨・孫骨の t 範囲

| パラメータ | 値 | 説明 |
|---|---|---|
| `subcauseTMin` | 0.18 | 小骨 t の下限（安全キャップ／自由端側の境界） |
| `subcauseTMax` | 0.84 | 小骨の展開起点（中骨の付け根 = 大骨側） |
| `subcauseStaggerRatio` | 0.68 | 内側小骨で孫骨を持たない場合の追加短縮比率 |
| `detailTMin` | 0.30 | 孫骨の展開起点（小骨の付け根 = 中骨側） |
| `detailTMax` | 0.76 | 孫骨 t の上限（安全キャップ／自由端側の境界） |

### 4.5 骨の長さ

| パラメータ | 値 | 説明 |
|---|---|---|
| `causeBaseLength` | 180 | 中骨の基本長 |
| `causeLengthPerPair` | 80 | 中骨: 小骨ペアが 1 組増えるごとの追加長 |
| `subcauseLength` | 108 | 小骨の基本長 |
| `subcauseLengthPerPair` | 30 | 小骨: 孫骨ペアが 1 組増えるごとの追加長 |
| `detailLength` | 56 | 孫骨の長さ（固定） |
| `subcauseLengthWithDetails` | 132 | ※互換用の予約値（現在未使用） |

### 4.6 間隔・マージン

| パラメータ | 値 | 説明 |
|---|---|---|
| `causeSpacingAlongBone` | 96 | 片側配置フォールバック時の中骨間の目安間隔 |
| `subcauseSpacingAlongCause` | 80 | ※予約値（現在未使用） |
| `innerSafeMargin` | 26 | 内側（背骨側）に伸びる骨が背骨・大骨ラインに対して確保する最小余白 |
| `causeLabelGap` | 18 | 中骨ラベルの縦方向オフセット基準 |
| `subcauseLabelGap` | 12 | 小骨ラベルの余白 |
| `detailLabelGap` | 6 | 孫骨ラベルの余白 |
| `categoryGap` | 90 | ※予約値（現在未使用） |
| `effectGap` | 18 | 背骨矢先と特性ボックスの間隔（教科書ルール：接する） |
| `spineEndPadding` | 110 | ※予約値（現在未使用） |
| `sideMarginX` | 70 | キャンバス左右の外側マージン |
| `verticalMargin` | 56 | キャンバス上下の外側マージン |

### 4.7 ボックス・キャンバス

| パラメータ | 値 | 説明 |
|---|---|---|
| `categoryBoxWidth` | 140 | カテゴリボックスの基本幅（ラベルが長い場合は拡幅） |
| `categoryBoxHeight` | 48 | カテゴリボックスの基本高さ（ラベルが折り返す場合は拡高） |
| `effectBoxMaxHeight` | 360 | ※予約値（現在未使用） |
| `minCanvasHeight` | 520 | キャンバスの最小高さ（空データでも視覚を保つ） |
| `minCanvasWidth` | 900 | ※予約値（現在未使用） |
| 最低アスペクト比 | 1.15 | コード内定数 `minAspect`。これを下回る場合は水平パディングを追加 |

### 4.8 フォントサイズ (`fontPx`)

| 階層 | サイズ (px) |
|---|---|
| `effect`（特性） | 24 |
| `category`（カテゴリ） | 18 |
| `cause`（中骨） | 15 |
| `subcause`（小骨） | 13 |
| `detail`（孫骨） | 12 |

### 4.9 ラベル折り返し幅 (`labelWrapWidth`、単位 px)

| 階層 | 幅 | 目安文字数（全角） |
|---|---|---|
| `cause` | 180 | 約 12 文字 |
| `subcause` | 143 | 約 11 文字 |
| `detail` | 108 | 約 9 文字 |
| `category` | 200 | 約 14 文字（超えると 2 行、ボックス自体も拡幅） |

### 4.10 線の太さ・矢印サイズ（描画関数内の定数）

| 階層 | 線の太さ | 矢印サイズ |
|---|---|---|
| 背骨 | 6.0 | 20 |
| 大骨 | 4.2 | 12 |
| 中骨 | 2.6 | 10 |
| 小骨 | 1.8 | 8 |
| 孫骨 | 1.2 | 6 |

---

## 5. 色・スタイル

### 5.1 基本カラー (`this.style`)

| キー | 色 | 用途 |
|---|---|---|
| `spine` | `#1f2d3d` | 背骨 |
| `major` | `#2c3e50` | （予備） |
| `cause` | `#4d5d6e` | 中骨の線 |
| `subcause` | `#75828f` | 小骨の線 |
| `detail` | `#93a0aa` | 孫骨の線 |
| `effectFill` / `effectStroke` | `#c0392b` / `#922b21` | 特性ボックス |
| `categoryFill` / `categoryStroke` | `#2980b9` / `#1f618d` | （予備・パレット未使用時） |
| `textDark` | `#1b2631` | 中骨ラベル文字色 |
| `textMid` | `#2e4053` | 小骨ラベル文字色 |
| `textLight` | `#ffffff` | ボックス内の白文字 |
| `textMuted` | `#4d5a66` | 孫骨ラベル文字色 |
| `bg` | `#ffffff` | 背景 |

中骨以下（中骨・小骨・孫骨の線・文字）は階層構造を保つため、常にグレー階調で統一する。

### 5.2 カテゴリカラーパレット (`categoryPalette`)

大骨とカテゴリボックスのみ、カテゴリごとに以下の 8 色を順に循環させて色分けし、視覚的なグルーピングを強化する（落ち着いたトーンで彩度を揃え、色覚多様性にも配慮）。

| # | 塗り (fill) | 線 (stroke) | 色名 |
|---|---|---|---|
| 1 | `#2f6fb3` | `#245a92` | 青 |
| 2 | `#2e8b6e` | `#247258` | 緑 |
| 3 | `#b3762f` | `#946025` | 琥珀 |
| 4 | `#8e5aa8` | `#75488c` | 紫 |
| 5 | `#3a8fa3` | `#2e7686` | 青緑 |
| 6 | `#6a7b8c` | `#556575` | スレート |
| 7 | `#a3663a` | `#87542f` | 褐色 |
| 8 | `#5d8a4a` | `#4b713c` | 深緑 |

9 カテゴリ目以降は先頭（青）に戻って循環する。

### 5.3 重要要因マーカー

| 項目 | 値 |
|---|---|
| 楕円の線色 | `#d63031`（赤） |
| 塗り | なし（`fill: none`） |

---

## 6. 入力データの制限（`mermaid-parser.js`）

視認性のための安全上限として、以下の本数制限を自動適用する（超過分は切り捨て）。

| 項目 | 上限 |
|---|---|
| カテゴリあたりの中骨（原因）数 | 6 |
| 中骨あたりの小骨（副原因）数 | 4 |
| 小骨あたりの孫骨（詳細）数 | 3 |

カテゴリの並び順は「機械・人・材料・方法」の 4M を優先し、それ以外の名前のカテゴリは後ろに追加される。

---

## 7. 検証（`verify.js`）

62 種類のダミーデータパターンを Playwright で自動描画し、以下を機械的にチェックする。

| チェック項目 | 内容 |
|---|---|
| overflow | 描画要素が viewBox からはみ出していないか |
| text-overlap | ラベルの外接矩形同士が重なっていないか |
| label-cross-heavy-line | ラベルが背骨・大骨などの太い線（stroke-width ≥ 2.5）と交差していないか |
| `sym`（対称性） | 上下カテゴリの配置対称性 |
| `aspc`（アスペクト比） | キャンバスの縦横比 |
| `gapσT` / `gapσB` | 上段・下段カテゴリ間の間隔のばらつき（標準偏差） |

コード変更時は必ず `node verify.js` を実行し、全パターンが `clean` であることを確認してから反映する。

---

## 8. 設計上の力学（トレードオフ）

レイアウト調整では、以下の 3 点が常に競合する関係にある。設計判断はこの 3 点のバランスを取ることを目的とする。

1. **交互サイクルの厳密性**: 大骨に対する中骨は必ず L, R, L, R... の位置順で交互配置する（3.1, 3.5 節）。
2. **背骨・親骨付近の余白最小化**: 骨は親骨の起点側（背骨寄り／中骨の付け根）から必要な分だけ詰めて展開し、無駄な空白（スカスカ）を作らない（3.4, 3.6 節）。
3. **重なり回避**: 内容（ラベル長・小骨/孫骨の量）に応じた最低限のクリアランスを事前に確保しつつ、実行時の安全キャップ（3.7 節）を二段構えで用意し、テキストや線の重なりをゼロに保つ。

(1) を満たすには同じペアの左右で t 値（位置）を共有する必要があり、(2) を追求すると (3) の重なりリスクが上がる。現在の実装は、内容量に応じた左右割当ての最適化（3.5 節）と、全カテゴリでの位置統一（3.6 節）によって、この 3 点を高い水準で両立させている。
