# 特性要因図（石川ダイアグラム）自動生成 ルール・パラメータ仕様書

本書は `ishikawa-diagram.js` が特性要因図を自動生成する際の**描画ルール**と**レイアウトアルゴリズム**、および**全パラメータ**を明文化したものです。コードの実装と対応しており、レイアウト調整時のリファレンスとして使用します。

本仕様の中心原則:

> 同じ階層の骨をすべて同じ長さにするのではなく、**同じ距離体系・近接ルール・密度目標・長さバンド**を共有する。子骨の位置は比率 (t 値) ではなく**実距離 (px)** で決め、親骨の付け根側から必要な距離だけ詰めて配置する。背骨と最初の中骨の距離は原則 48px とし、大骨が長くなっても比例して拡大させない。

これにより、入力が少ない場合は引き締まった図となり、入力が多い場合は必要な部分だけが自然に拡張される。

---

## 1. 用語・階層構造

特性要因図は以下の 5 階層で構成されます。

| 階層 | 呼称 | 役割 | データ上のキー |
|---|---|---|---|
| 0 | 特性 (effect) | 分析対象の問題・結果。図の右端の赤い箱 | `effect` |
| 1 | 背骨 (spine) | 中心の太い水平矢印。特性ボックスへ向かう | - |
| 2 | 大骨 (major bone) | カテゴリ（4M: 機械・人・材料・方法 等）を表す斜めの骨 | `category` |
| 3 | 中骨 (cause) | 大骨に対する原因 | `cause` |
| 4 | 小骨 (subcause) | 中骨に対する副原因 | `subcause` |
| 5 | 孫骨 (detail) | 小骨に対する詳細要因 | `detail` |

各階層は「親骨」（1 つ上の階層の骨）に対して枝分かれする「子骨」であり、レイアウトルールは**同じ再帰パターン**を各階層に繰り返し適用する形で設計されています。

---

## 2. 描画の基本ルール（教科書ルール）

1. **背骨**: 太い水平の矢印。矢先は特性ボックスの左辺に接する。
2. **大骨**: 背骨に対して 60°（`majorAngleDeg`）で、カテゴリの並び順に上下交互に配置。矢先は背骨に突き刺さる。先端にカテゴリボックスを置く。
3. **中骨**: 背骨と平行（水平）。大骨の両側にペアで交互配置し、矢先は大骨へ向かう。ペアの左右は大骨上で段違い（`pairStaggerPx` = 24px）にし、矢印同士の衝突を防ぐ。
4. **小骨**: 大骨と平行（大骨と同じ 60°）。中骨の上下に交互配置し、矢先は中骨へ向かう。
5. **孫骨**: 中骨と平行（水平）。小骨のライン上に分岐し、矢先は小骨へ向かう。
6. **線の太さ**: 階層が下がるごとに単調減少する。
   - 背骨 6.0 → 大骨 4.2 → 中骨 2.6 → 小骨 1.8 → 孫骨 1.2
7. **矢印サイズ**: 背骨 20 → 大骨 12 → 中骨 10 → 小骨 8 → 孫骨 6

### 2.1 再帰的な交互展開ルール

> **「親骨が斜めなら子骨は左右交互、親骨が水平なら子骨は上下交互」**

この 1 つのルールを階層に関わらず繰り返し適用している。

| 親骨 | 親骨の向き | 子骨の交互方向 |
|---|---|---|
| 背骨 | 水平 | 大骨は上下交互（カテゴリの出現順） |
| 大骨 | 斜め (60°) | 中骨は左右交互 |
| 中骨 | 水平 | 小骨は上下交互 |
| 小骨 | 斜め (60°) | 孫骨は左右交互（※安全な場合のみ。2.2 参照） |

### 2.2 孫骨の左右交互展開の安全制限

孫骨は本来「小骨が斜め骨なので左右交互」となるはずだが、内側配置の小骨で行うと大骨ラインに干渉するケースがあるため、以下の条件を**すべて**満たす場合のみ交互展開を許可する。

- 中骨が **外側 (left)** に配置されている
- 小骨が **外側 (isOuterSub)** に配置されている
- 反転した孫骨（ラベル幅を含む実効到達距離）が大骨ラインに `effectiveInnerSafeMargin` 未満まで接近しない（`segmentTooCloseToLine` による幾何判定）
- 反転した孫骨が同じ側の隣接小骨の領域を侵さない（同じ側の小骨間隔の 46% 以内）

条件を満たさない場合は、孫骨は親小骨と同じ方向（安全側）に統一する。

### 2.3 中骨のペア組み・内側/外側アサイン（内容量ベース）

中骨は大骨の両側に「ペア」単位で配置され、ペア内の片方が外側 (left)、もう片方が内側 (right / 背骨側) になる。

- 全中骨を「内側に配置したときの必要水平距離」（`innerNeedOf` = 小骨配置スパン + 先端張り出し + 安全余白）の**昇順**にソートする。
- 軽いもの同士を**背骨に一番近いペア**へ、重いもの同士を**先端側の遠いペア**（元々余地が大きい）へまとめる。
- 各ペア内では、必要量が小さい方を**内側**、大きい方を**外側**にする。
- 相方のいない単独中骨（原因数が奇数の余り）は常に外側。

L, R, L, R... という**位置の交互サイクル**は大骨上の位置の順序であり、その位置をどの中骨（データ上のどの原因）が占めるかとは独立なので、この並び替えを行っても交互サイクルは崩れない。

---

## 3. 空間・距離最適化レイアウト

### 3.1 設計の基本原則

1. **近接の原則**: 関係の強い要素は近づけ、異なるグループ間には明確な間隔を設ける。特に「背骨から最初の中骨までの距離」は**大骨長に対する割合ではなく実距離 (px)** で管理する（旧方式 `位置 = 大骨長 × t` は大骨が長くなるほど背骨との空白が拡大するため廃止）。
2. **リズムの原則**: 主要距離は 8px 基本単位（`baseUnit`）へ丸め、図全体に一定の視覚リズムを持たせる。
3. **層単位の長さ統一の原則**: 各骨の必要長は、含まれている全要素（子のラベル幅・孫骨の配置スパン・自身のラベル）から**正確に**算出する（本数だけで決めない）。その上で、**同じ層の骨は図全体で最大必要長へ統一する**（中骨層・小骨層・大骨層それぞれ 1 つの長さ）。位置は実距離パッキングで決まるため、長さを統一しても内容が間延びしない。
4. **大骨長の統一**: 大骨の長さは図全体で最大必要長へ統一し、大骨先端（カテゴリボックス）の位置をカテゴリ間で揃える。中骨の位置は「背骨からの実距離」で決まり大骨長に比例しないため、大骨を伸ばしても内容は背骨側へ詰まったままで間延びしない — 伸びるのは中身のない骨の先端部分だけである（旧 t 比率方式のように内容ごと引き伸ばされる問題は起きない）。
5. **内容密着キャンバス**: キャンバスは内容の外接矩形に密着させる。上下の必要高さが大きく異なる場合は背骨をキャンバス中央に固定せず、上下それぞれの実必要量で高さを取る（軽い側にも重い側の 40% は確保）。

### 3.2 レイアウト計算のフェーズ構成 (`computeLayout`)

1. **Phase 1（内容量計測）**: 各中骨について、ラベル幅（折返し込み）・小骨/孫骨の本数・孫骨配置に必要な小骨長（`subOwnLen`）・小骨配置に必要な中骨長（`causeOwnLen`）を**その骨自身の内容**から個別に計算する。
2. **Phase 2（層単位統一）**: 中骨長 = 全中骨の必要長の最大値、小骨長 = 全小骨の必要長の最大値として、それぞれ図全体で統一する（8px 量子化）。
3. **Phase 3（中骨の絶対距離配置）**: ペア組み（2.3 節）後、各ペアの大骨上の位置（背骨からの実距離 `pairCenters[k]`）を決定する（3.3 節）。大骨必要長 = 最遠 attach 距離 + 終端余白。
4. **Phase 4（大骨長の統一）**: 全カテゴリの大骨長を最大必要長へ統一（ボックス位置が揃う）。
5. **Phase 5（キャンバス確定）**: 縦横張り出しの実測からキャンバスサイズを決定。列単位で spineX を共有し、上下対称・左右整列を保つ。
6. **座標確定**: `computeCategoryGeometry` 以下で全骨の実座標を計算する（すべて px 直接計算。t 比率は使わない）。

### 3.3 中骨の配置（厳密ジオメトリ解決）

ペア位置と大骨必要長は、占有量の合算のようなヒューリスティックではなく、**実際に描画される全要素（骨線・ラベル外接矩形・重要マーク楕円）の厳密な分離条件**から数学的に確定する（`solveCategoryExactGaps`）。

原理: スロット（ペア）k の全要素は、attach 距離を δ 増やすと `u = (−cos60°, ∓sin60°)` 方向へ剛体的に平行移動する。「要素 A（固定）と要素 B（移動）が `gapPaddingPx` 以上離れる最小の δ」は、各軸の区間重なり条件を δ の 1 次不等式として解いた重なり区間 `(lo, hi)` の上端として**閉形式**で求まる（`minSeparationPush`）。制約は 3 種:

- **(a) 背骨ライン**: 全要素が背骨から実効安全距離以上離れる（δ ≥ (要素下端 + 安全距離) / sin60°）。背骨は**水平線**であり垂直の壁ではないため、内側中骨の内容は大骨の背骨接続点 X を右へ `innerOverhangPx` (288px ≒ 垂直 500px 相当) まで越えてよい — この空間 (隣の列との間) の活用が、中骨群を背骨へ大きく寄せる鍵。実張り出し (`rightOverhangPx`、要素外接矩形から実測) は列間隔と背骨終端の計算で厳密に吸収する
- **(b) スロット間**: 手前の全スロットの要素集合と `gapPaddingPx` 以上分離する
- **(c) カテゴリボックス**: ボックス矩形が全要素と分離する → 大骨必要長が確定

要素の外接矩形は描画コードと同一の式（`collectCauseElementBoxes` / `labelBlockBBox`）で生成し、斜め線分は短区間に分割して AABB の過大評価を防ぐ。内側中骨の実行時キャップは attach 距離に依存する（剛体でない）ため、ジオメトリを再構築しながら固定点まで反復する（単調増加で収束、最大 6 回）。

シード（下限）: `pairCenters[0] = preferredFirstCauseGap (48px)`、以降は `+pairGapMin`。内側原因の水平クリアランス `center ≥ innerNeedOf / cos60° − pairStaggerPx` のみ事前確保（実行時キャップによる小骨圧縮を防ぐため）。最後に 8px 単位へ切り上げ。ペア内の実 attach 位置は、外側 = `center`、内側 = `center + pairStaggerPx`。

**原因が 1 本のカテゴリ**は `preferredFirstCauseGap` を基準に外側配置し、背骨クリアランス (a) が必要な分だけ厳密に移動する。

### 3.4 小骨・孫骨の配置（インターリーブ絶対距離）

小骨は「中骨の付け根（大骨側 = 親の親に近い側）」から `firstSubcauseGap` (48px) を起点に自由端方向へ詰める。どの小骨をどちらの側に置くかは内容量で決める: **孫骨の少ない/無い小骨 floor(n/2) 本を内側（背骨側）へ、孫骨持ちの重い小骨を外側へ**（外側では付け根から遠いほど内容が重くなるよう昇順）。内側は 0.68 短縮で描かれるため軽い小骨が適し、孫骨ラベルが同じ側の隣の小骨の先端ラベルへ届く事故も防げる。上下交互サイクル（位置の交互）自体は保たれる。上下交互のため衝突リスクは**同じ側 (index 差 2)** の小骨同士のみ — 同じ側にはラベル幅ベースの安全間隔（`maxSubLabel + 24`）、隣り合う反対側には小さな段差（`subStagger` = 32px）だけを与える（`computeInterleavedDistances`）。

孫骨は「小骨の付け根（中骨側）」から `firstDetailGap` (32px) を起点に、ラベル幅ベースの間隔で詰める。

いずれも親骨の自由端には親骨自身のラベルが置かれるため、終端余白（小骨: 40px / 孫骨: 32px）を確保する。親骨長はこの配置に必要な長さを Phase 1 で見込んで決まっているため、通常は圧縮が発生しない。収まらない極端なケースのみ全体を比例圧縮する（per-step クランプによる完全重なりは起こさない）。

### 3.5 骨長の決定（全要素ベースの必要長 + 層単位の最大値統一）

```
小骨の必要長 = max(subcauseBaseLength,
                   firstDetailGap + (孫骨数-1)×孫骨ラベル幅ベース間隔 + 32)
中骨の必要長 = max(causeBaseLength,
                   小骨配置スパン (インターリーブ実測),
                   中骨ラベル幅 + 40)
大骨の必要長 = 最遠attach距離 + categoryEndClearanceMin を起点に、
               カテゴリボックス矩形と全要素の厳密分離 (3.3(c)) で確定
```

その後、小骨長・中骨長・大骨長それぞれを**図全体の最大必要長へ統一**する（Phase 2 / Phase 4）。中骨長には小骨の斜めチェーン（`小骨長×cos60°` や孫骨の張り出し）を**含めない** — 斜めチェーンは骨の軸から離れる方向へ伸びるため骨の長さを消費せず、キャンバス側の張り出し（`beyondAttachHorizontal`）で別途確保する。

> **注意**: 骨長の決定にラベル幅を混ぜるのは「その骨自身のラベル」まで。他の骨のラベルの都合で骨を伸ばさない。ラベルが骨より長い場合は先端からはみ出して描画されるだけで正しい。

### 3.6 内側（背骨側）骨の安全制御

- 実効安全距離: `effectiveInnerSafeMargin = max(innerSafeMargin(24), 小骨フォント×1.4, 矢印+8)`
- 原則は**事前確保**（3.3 節の下限で接続位置を移動）。実行時キャップ（内側中骨: 背骨に届かない長さへ制限 / 内側小骨: 背骨 y に届かない長さへ制限 + 孫骨なしは `subcauseStaggerRatio`=0.68 で短縮）は**最終安全弁**であり、通常入力では発動しないことを目標とする。

### 3.7 キャンバス・アスペクト比

- 上下の必要高さを個別に計算し、背骨 y = 上側必要高。軽い側にも重い側の 40% を確保（ソフト対称）。
- 列間隔は `columnGapPreferred` (64px)。8px 単位。
- アスペクト比の目標範囲 1.20〜1.85 は**参考値**。補正のための追加余白は `min(キャンバス幅の 6%, 96px)` を上限とし、それ以上の空白追加は行わない（内容密着を優先）。

### 3.8 ラベルの折り返し

`labelWrapWidth` を超えるラベルは文字幅推定に基づいて最大 2 行にバランス分割（`wrapLabel`）。カテゴリ名も折り返し、長い場合はボックス自体を拡幅・拡高する。文字幅推定は半角 ASCII ≈ 0.55em、全角 ≈ 1.0em。

### 3.9 重要要因マーカー（赤丸）

`cause` / `subcause` / `detail` のラベル末尾に `!`（または全角 `！`）を付けると赤い楕円で囲まれる（QC の慣習）。パースは `mermaid-parser.js` の `parseImportance()`。

---

## 4. パラメータ一覧 (`this.params`)

### 4.1 角度

| パラメータ | 値 | 説明 |
|---|---|---|
| `majorAngleDeg` | 60 | 大骨の背骨に対する傾き |
| `subcauseAngleDeg` | 60 | 小骨の傾き（大骨と平行） |

### 4.2 距離体系（実距離 px、8px リズム）

| パラメータ | 値 | 説明 |
|---|---|---|
| `baseUnit` | 8 | 距離の量子化単位 |
| `preferredFirstCauseGap` | 48 | 背骨 → 最初の中骨ペアの基準距離 |
| `minimumFirstCauseGap` | 40 | 同、下限 |
| `pairGapMin` | 56 | 隣接ペア間の最小 along-bone 間隔 |
| `pairStaggerPx` | 24 | ペア内の外側/内側の attach 距離差 |
| `firstSubcauseGap` | 48 | 中骨の付け根 → 最初の小骨 |
| `subStagger` | 32 | 隣り合う反対側 (上/下) 小骨の段差 |
| `firstDetailGap` | 32 | 小骨の付け根 → 最初の孫骨 |
| `columnGapPreferred` | 64 | カテゴリ列間の基準間隔 |
| `gapPaddingPx` | 12 | 厳密ジオメトリ解決における要素間の最小分離距離（唯一の美観ノブ） |
| `innerOverhangPx` | 288 | 内側中骨の内容が背骨接続点 X を右へ越えてよい距離（垂直 500px 相当） |

### 4.3 骨長・バンド

| パラメータ | 値 | 説明 |
|---|---|---|
| `causeBaseLength` | 168 | 中骨の最小長 |
| `subcauseBaseLength` | 104 | 小骨の最小長 |
| `subcauseStaggerRatio` | 0.68 | 内側小骨（孫骨なし）の追加短縮比率 |
| `detailLength` | 56 | 孫骨の長さ（固定） |
| `lengthBandPx` | 16 | 中骨・小骨長の量子化単位 |
| `branchUnifyTolerance` | 0.15 | カテゴリ内バンド統合しきい値 |
| `maxLengthBandsPerCategory` | 3 | カテゴリ内の長さバンド上限 |

### 4.4 余白・ボックス・キャンバス

| パラメータ | 値 | 説明 |
|---|---|---|
| `innerSafeMargin` | 24 | 内側骨の基礎安全距離（実効値は 3.6 節） |
| `categoryEndClearanceMin` | 112 | 最後の中骨〜カテゴリボックスの最小余白 |
| `categoryBoxWidth` / `categoryBoxHeight` | 140 / 48 | カテゴリボックスの最小サイズ（ラベルに応じ拡張） |
| `effectGap` | 18 | 背骨矢先と特性ボックスの間隔 |
| `horizontalMargin` / `verticalMargin` | 48 / 40 | キャンバス外側マージン |
| `maxAspectPadding` | 96 | アスペクト補正の追加余白上限 (px) |
| `maxAspectPaddingRatio` | 0.06 | 同、幅比率上限 |
| `minCanvasHeight` | 520 | キャンバス最小高さ |
| `causeLabelGap` / `subcauseLabelGap` / `detailLabelGap` | 18 / 12 / 6 | 各ラベルの余白 |

### 4.5 フォントサイズ (`fontPx`)

| 階層 | サイズ (px) |
|---|---|
| `effect` | 24 |
| `category` | 18 |
| `cause` | 15 |
| `subcause` | 13 |
| `detail` | 12 |

### 4.6 ラベル折り返し幅 (`labelWrapWidth`)

| 階層 | 幅 (px) | 目安（全角） |
|---|---|---|
| `cause` | 180 | 約 12 文字 |
| `subcause` | 143 | 約 11 文字 |
| `detail` | 108 | 約 9 文字 |
| `category` | 200 | 約 14 文字 |

### 4.7 線の太さ・矢印サイズ（描画関数内の定数）

| 階層 | 線の太さ | 矢印サイズ |
|---|---|---|
| 背骨 | 6.0 | 20 |
| 大骨 | 4.2 | 12 |
| 中骨 | 2.6 | 10 |
| 小骨 | 1.8 | 8 |
| 孫骨 | 1.2 | 6 |

---

## 5. 色・スタイル

### 5.1 基本カラー (`this.style`)

| キー | 色 | 用途 |
|---|---|---|
| `spine` | `#1f2d3d` | 背骨 |
| `cause` | `#4d5d6e` | 中骨の線 |
| `subcause` | `#75828f` | 小骨の線 |
| `detail` | `#93a0aa` | 孫骨の線 |
| `effectFill` / `effectStroke` | `#c0392b` / `#922b21` | 特性ボックス |
| `textDark` / `textMid` / `textMuted` | `#1b2631` / `#2e4053` / `#4d5a66` | 中骨/小骨/孫骨ラベル |
| `textLight` | `#ffffff` | ボックス内の白文字 |
| `bg` | `#ffffff` | 背景 |

中骨以下（線・文字）は階層構造を保つため、常にグレー階調で統一する。

### 5.2 カテゴリカラーパレット (`categoryPalette`)

大骨とカテゴリボックスのみ、カテゴリごとに 8 色を循環（青・緑・琥珀・紫・青緑・スレート・褐色・深緑）。落ち着いたトーンで彩度を揃え、色覚多様性にも配慮。

### 5.3 重要要因マーカー

赤い楕円 `#d63031`、塗りなし。

---

## 6. 入力データの制限（`mermaid-parser.js`）

| 項目 | 上限 |
|---|---|
| カテゴリあたりの中骨（原因）数 | 6 |
| 中骨あたりの小骨（副原因）数 | 4 |
| 小骨あたりの孫骨（詳細）数 | 3 |

カテゴリの並び順は 4M（機械・人・材料・方法）を優先し、それ以外は後ろに追加。

---

## 7. 検証（`verify.js`）

62 種類のダミーデータパターンを Playwright で自動描画し、以下を機械的にチェックする。

| チェック項目 | 内容 |
|---|---|
| overflow | 描画要素が viewBox からはみ出していないか |
| text-overlap | ラベルの外接矩形同士が重なっていないか |
| label-cross-heavy-line | ラベルが背骨・大骨などの太い線と交差していないか |
| `sym` / `aspc` / `gapσ` | 対称性・アスペクト比・カテゴリ間隔の指標 |

コード変更時は必ず `node verify.js` を実行し、全パターンが `clean` であることを確認してから反映する。

---

## 8. 期待動作

### 8.1 単純構造

- 最初の中骨は背骨から約 48px（安全確保が必要な場合のみ拡張）
- 中骨を大骨全体へ均等分散しない
- カテゴリボックスを必要以上に遠ざけない
- 大骨長は図全体で統一されるが、中骨の位置は実距離基準のため内容は間延びしない
- 内容を背骨側へコンパクトにまとめる
- 原因 1 本のカテゴリも固定 t 範囲を使わず `firstCauseGap` 基準で外側配置

### 8.2 複雑構造

空間は次の順で確保する: 子骨間の必要距離を実測 → 親骨を延長 → ペア間隔を必要量だけ拡大 → 列間隔を拡大 → キャンバスを拡張。複雑な要因だけを局所的に拡張し、他の単純なカテゴリまで同じ長さにしない。安全上必要な距離を縮小して固定範囲へ押し込むことはしない。

### 8.3 設計の優先順位

1. 重なりを発生させない
2. 親子関係を明確にする
3. 背骨付近の空白を最小化する
4. 子骨を付け根側から詰めて配置する
5. 視覚密度を一定範囲へ収める
6. 必要箇所だけを局所的に拡張する
7. 骨長ではなく距離リズムを統一する
8. 完全対称よりも視覚的均衡を優先する
9. キャンバスを内容へ密着させる
10. 単純構造を複雑構造の最大値へ引っ張らない
